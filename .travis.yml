sudo: required
services:
  - docker

before_install:
  - docker build -t alexandrusimionovro/docker-react -f Dockerfile.dev .

script:
  - docker run alexandrusimionovro/docker-react npm run test -- --coverage 
